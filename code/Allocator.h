#ifndef ALLOCATOR_H
#define ALLOCATOR_H

// permanent memory: will be static and partitioned
// scene memory: loads when loading a scene, and unloads fully when
//   switching a scene; it should be vritualized with a dirty bit
// temporary/scratch memory: flushed after each frame; should have
//   continuous allocation




#endif

